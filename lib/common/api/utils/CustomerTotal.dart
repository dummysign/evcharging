import '../../../app/data/Customer.dart';

extension CustomerTotal on Customer {
 /* double get totalKhata {
    double sum = 0;
    for (var entry in ledger) {
      sum += (entry['price'] ?? 0).toDouble();
    }
    return sum;
  }*/
}
